name: Say hello

on:
  push:
    branches: 
      - main

jobs:
  hello_word:
    runs-on: ubuntu-18.04
    steps:
      - name: Echo the message
        run: |
          echo "hola mindu!"
      - name: Dime la fecha
        run: date
      - run: ls -l